//import 'package:share_plus/share_plus.dart';

class ShareService {
  /// Поделиться ссылкой
  static Future<void> shareLink(String url, {String? subject}) async {
    // await SharePlus.instance
    //     .share(ShareParams(uri: Uri.parse(url), subject: subject));
  }
}
